{{- $ab := index .Values "auth-backend" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-backend
spec:
  replicas: {{ $ab.replicaCount }}
  selector:
    matchLabels:
      app: auth-backend
  template:
    metadata:
      labels:
        app: auth-backend
    spec:
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret }}
      initContainers:
        - name: wait-for-postgres
          image: busybox
          command:
            - sh
            - -c
            - until nc -z {{ .Release.Name }}-postgresql 5432; do echo "waiting"; sleep 2; done
        - name: run-migrations
          image: "{{ $ab.image.repository }}:{{ $ab.image.tag }}"
          command: ["/bin/sqlx", "migrate", "run", "--source", "/migrations"]
          env:
            - name: DATABASE_URL
              value: "postgres://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ .Release.Name }}-postgresql:5432/{{ .Values.postgresql.auth.database }}"
      containers:
        - name: auth-backend
          image: "{{ $ab.image.repository }}:{{ $ab.image.tag }}"
          imagePullPolicy: {{ $ab.image.pullPolicy }}
          ports:
            - containerPort: {{ $ab.port }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ $ab.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          env:
            - name: PORT
              value: {{ $ab.port | quote }}
            - name: RUST_LOG
              value: {{ $ab.env.RUST_LOG }}
            - name: DATABASE_URL
              value: "postgres://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ .Release.Name }}-postgresql:5432/{{ .Values.postgresql.auth.database }}"
            - name: HASH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: HASH_SECRET
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: JWT_SECRET
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: ENCRYPTION_KEY
            - name: INTERNAL_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: INTERNAL_API_KEY
